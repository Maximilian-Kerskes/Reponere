# Reponere
Reponere is a source-code based package manager written entirely in rust.

Packages are compiled and stored locally, being build as specified in a self-defined package_build.yaml/yml file.

## TODO's
- Allow for interactivity
- Implement install_location in package_build
- Implement more commands
- Add more source types
- Add documentation
