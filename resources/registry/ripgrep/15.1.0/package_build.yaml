name: ripgrep 
version: "15.1.0"
description: "rusty grep"

source:
  !Git
  repo: "https://github.com/BurntSushi/ripgrep.git"

dependencies:
  runtime:
  build:

build:
  steps:
    - "cargo build --release"
    - "cp target/release/rg ~/.local/bin/rg"

install_path: ".local/bin/rg"
